import test from 'node:test';import assert from 'node:assert/strict';
import {isFreshViral,assess} from './logic.js';
const now=Date.parse('2026-09-14T12:00:00Z');
test('Viral status requires evidence and expires after seven days',()=>{const p={trend:'Viral'};assert.equal(isFreshViral(p,now),false);p.trendEvidence={qualified:true,sourceUrl:'https://example.com/item',summary:'Dated velocity',observedAt:'2026-09-14',periodEnd:'2026-09-13'};assert.equal(isFreshViral(p,now),true);p.trendEvidence.periodEnd='2026-08-31';assert.equal(isFreshViral(p,now),false);p.trendEvidence.periodEnd='2026-10-01';assert.equal(isFreshViral(p,now),false)});
test('A category gap is never treated as confirmed whitespace',()=>{const p={id:'a',name:'New sponge',brand:'A',category:'Sponges',tags:[]};const a=assess(p,[p],now);assert.equal(a.status,'Not verified');assert.equal(a.ready,false);assert.equal(a.checked,0)});
test('Related Target format provides a comparison, not equivalence',()=>{const p={id:'a',name:'Soft sponge',brand:'A',category:'Sponges',tags:[]};const q={id:'b',name:'Sponge 2pk',brand:'B',category:'Sponges',retailer:'Target',channel:'Target',link:'https://www.target.com/p/item',tags:[]};const a=assess(p,[p,q],now);assert.equal(a.status,'Target alternatives to check');assert.equal(a.similar[0].id,'b');assert.equal(a.ready,false);assert.equal(assess(q,[p,q],now).status,'Target listing recorded')});
